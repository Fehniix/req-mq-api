{
  "name": "req-mq-api",
  "version": "0.0.2",
  "description": "An API provider based on BullMQ (Redis) that allows Promise-based inter-process communication to make HTTP-like GET and POST requests.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "npm run clean && tsc --build",
    "test": "export TEST=true&& npm run build && mocha --full-trace --require ./dist/test/setup.js ./dist/test/*.test.js ./dist/test/**/*.test.js && export TEST=false",
    "test-dev": "export DEBUG=req-mg-api:*&& npm test && export DEBUG="
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Fehniix/req-mq-api.git"
  },
  "keywords": [
    "api",
    "inter-process",
    "process",
    "communication",
    "bullmq",
    "redis"
  ],
  "author": "Johnny Bueti",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Fehniix/req-mq-api/issues"
  },
  "homepage": "https://github.com/Fehniix/req-mq-api#readme",
  "dependencies": {
    "bullmq": "^1.57.3",
    "chai": "^4.3.4",
    "ioredis": "^4.28.2",
    "mocha": "^9.1.3"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0"
  }
}
